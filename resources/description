no escaping in str, so symbol " is illegal

fn - defining function
1 arg - function name
2 arg - arguments in format ({variable_name}[, ...])
3 arg - expression
example: (fn max (a b) (case (> a b) a b))
call function example (max 15 10)
fn expression return 0

case
1 arg - condition expression
2 arg - if value is not zero expression
3 arg - else expression
example: (case (% i 2) (print "i is odd") (print "i is even"))

for
1 arg - variable name; variable's value is initially 0
2 arg - expression that returns element's value; have access to previous element's value
3 arg - condition expression; continue loop execution if returned true
4 arg - expression
example: (for i (+ i 5) (< i 100) (print_int i))
for expression returns sum of expressions

built-ins:
(fn sign (a)) - returns 1 if negative, 0 else
(fn & (a b))
(fn | (a b))
(fn + (a b))
(fn - (a b))
(fn * (a b))
(fn / (a b))
(fn % (a b))
(fn in ()) - returns single char from buffer
(fn out (a)) - recieves char and returns 0
(fn print (a)) - recieves pointer to str and returns str length
(fn print_positive_int (a)) - prints positive integer without leading zeroes